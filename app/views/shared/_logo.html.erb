<!-- Uplo Logo -->
<%= link_to authenticated? ? photos_path : root_path, class: "hover:opacity-80 transition-opacity #{local_assigns[:color] || 'text-accent'}", style: "font-family: system-ui, -apple-system, sans-serif; font-size: #{local_assigns[:size] || '2rem'}; font-weight: 800; letter-spacing: -0.05em;" do %>
  UPLO
<% end %>