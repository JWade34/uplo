<!-- Uplo Logo -->
<%= link_to authenticated? ? photos_path : root_path, class: "hover:opacity-80 transition-opacity flex items-center #{local_assigns[:color] || 'text-accent'}" do %>
  <%= image_tag "uplo-logo.svg", alt: "Uplo", class: "h-8", style: "height: #{local_assigns[:size] || '2rem'}; width: auto; color: inherit;" %>
<% end %>